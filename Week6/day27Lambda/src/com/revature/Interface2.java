package com.revature;
//Example for functional interface
public interface Interface2 {

	//by default all the methods in interface are abstract [ No need to use abstract keyword in interface]
	void display();
}
